<!DOCTYPE html>
<html>
<head>
  <link rel="stylesheet" href="/stylesheets/amusepark.css">
</head>
<body>
  <div class="container">
    <h2>Admission Control</h2>
    <table>
      <tr  class="table-header">
        <th>TicketID</th>
        <th><a href="/ticketType">Type</a></th>
        <th>Price</th>
        <th>PurchaseDate</th>
        <th>PurchaseBy</th>
        <!-- <th>Edit</th>
        <th>Delete</th> -->
      </tr>
      <% rows.forEach(function(r) { %>
      <tr  class="table-row">
        <td><%= r.ticketid %></a></td>
        <td><%= r.type %></td>
        <td><%= r.price %></td>
        <td><%= r.purchasedate %></td>
        <td><%= r.touristid %></td>
        <!-- <td class="button">
          <form action="/tourist/<%=r.touristid%>" method="post">
            <button type="submit">Edit</button>
          </form>
        </td>
        <td class="button">
          <form action="/tourist/<%=r.touristid%>" method="post">
            <button type="submit">Delete</button>
          </form>
        </td> -->
      <% }); %>
    </table>
  </div>
  <form action="/homepage" method="get">
    <button class="homepage" type="submit"> Back to Home Page</button>
  </form>
</body>
</html>